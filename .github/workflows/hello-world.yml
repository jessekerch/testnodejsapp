name: Hello World!
on:
  workflow_dispatch

jobs:
  push_to_registry:
    name: Say Hello from Team5!
    runs-on: ubuntu-latest
    steps:
      - name: Team5 Hello World JS Action
        uses: jessekerch/hello-world-action@helloworld
            
